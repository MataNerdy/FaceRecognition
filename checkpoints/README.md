# Checkpoints

Веса моделей не хранятся в git. Сохраняйте обученные `.pth`, `.pt` и `.ckpt` файлы в этой директории или передавайте другой путь через CLI.

Локальный вес `shg_amazing.pth` вынесен за пределы репозитория в соседнюю папку `FaceRecognition_local_artifacts/`.
